# ─── Application ───
APP_NAME=Compliance Management System
APP_VERSION=1.0.0
DEBUG=false

# ─── Database ───
DATABASE_URL=postgresql+asyncpg://postgres:root@localhost:5432/compliance-management-system
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10

# ─── JWT Authentication ───
JWT_SECRET_KEY=your-super-secret-key-change-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ─── Logging ───
LOG_LEVEL=INFO
LOG_FILE_PATH=logs/application.log
LOG_MAX_BYTES=10485760
LOG_BACKUP_COUNT=10

# ─── Redis ───
REDIS_URL=redis://localhost:6379/0

# ─── Celery ───
CELERY_BROKER_URL=redis://localhost:6379/1

# ─── OpenTelemetry ───
OTEL_EXPORTER_ENDPOINT=http://localhost:4317
OTEL_SERVICE_NAME=compliance-api

# ─── Azure AD / Microsoft SSO ───
AZURE_CLIENT_ID=your-azure-client-id
AZURE_CLIENT_SECRET=your-azure-client-secret
AZURE_TENANT_ID=your-azure-tenant-id
AZURE_REDIRECT_URI=http://localhost:6769/oauth/v2/callback

# ─── CORS ───
CORS_ORIGINS=["http://localhost:6769"]

# ─── Auth Cookies (refresh token) ───
# COOKIE_SECURE must be true in production (HTTPS). Keep false for local http dev.
# For cross-site (API on a different domain than the SPA) use COOKIE_SAMESITE=none + COOKIE_SECURE=true.
REFRESH_COOKIE_NAME=refresh_token
REFRESH_COOKIE_PATH=/api/v1/auth
COOKIE_SECURE=false
COOKIE_SAMESITE=lax
COOKIE_DOMAIN=
